name: Lokalise Key Push
description: 'Pushes new translations keys to Lokalise'
inputs:
  api-token:
    description: API token
    required: true
  project-id:
    description: The project ID
    required: true
  glob-pattern:
    description: The glob pattern of files containing keys
    required: true
  key-name-property:
    default: web
    
runs:
  using: 'node12'
  main: 'index.js'